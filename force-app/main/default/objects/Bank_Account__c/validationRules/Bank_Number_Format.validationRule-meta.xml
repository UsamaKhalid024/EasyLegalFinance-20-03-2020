<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Bank_Number_Format</fullName>
    <active>true</active>
    <description>Bank Numbers must start with a 0 and contain 4 digits</description>
    <errorConditionFormula>IF( REGEX(Bank__c, &quot;^[0]{1}[0-9]{3}$&quot;), FALSE, TRUE)</errorConditionFormula>
    <errorDisplayField>Bank__c</errorDisplayField>
    <errorMessage>Bank Numbers must start with a 0 and contain 4 digits</errorMessage>
</ValidationRule>
