<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Account_Only_Number_and_Separators</fullName>
    <active>true</active>
    <description>Tests if Account Number only contains Numbers, Spaces, or Hyphens.</description>
    <errorConditionFormula>IF( REGEX(Account__c, &quot;^[0-9 -]*$&quot;), FALSE, TRUE)</errorConditionFormula>
    <errorDisplayField>Account__c</errorDisplayField>
    <errorMessage>The Account Number can only contain numbers, spaces, and hyphens.</errorMessage>
</ValidationRule>
